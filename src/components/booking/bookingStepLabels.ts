/** Hospitality reservation labels — progress aria only, never funnel copy. */
export const BOOKING_STEP_LABELS = ['Arrival', 'Travellers', 'Your details', 'Request'] as const;

export const BOOKING_STEP_COUNT = BOOKING_STEP_LABELS.length;
