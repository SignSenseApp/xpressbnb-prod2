/** Shared booking flow step labels — used by progress bar and step pills. */
export const BOOKING_STEP_LABELS = ['Dates', 'Guests', 'Contact', 'Send'] as const;

export const BOOKING_STEP_COUNT = BOOKING_STEP_LABELS.length;
